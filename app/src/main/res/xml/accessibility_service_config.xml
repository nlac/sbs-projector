<?xml version="1.0" encoding="utf-8"?>
<!--
    Configuration for SbsAccessibilityService.
    accessibilityEventTypes="typeNone" — we don't need to observe any UI events.
    canPerformGestures="true"          — required for dispatchGesture() to work.

    android:accessibilityEventTypes="typeAllMask"

-->
<accessibility-service
    xmlns:android="http://schemas.android.com/apk/res/android"
    accessibilityEventTypes="typeWindowStateChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault"
    android:canPerformGestures="true"
    android:canRetrieveWindowContent="true"
    android:description="@string/accessibility_service_description" />
